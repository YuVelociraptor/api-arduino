[project]
name = "api-arduino"
version = "0.1.0"
description = "API Server for arduino"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.116.1",
    "uvicorn>=0.35.0",
]
