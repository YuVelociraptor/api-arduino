services:
  fastapi:
    build:
      context: .
      dockerfile: docker/python/Dockerfile
    container_name: api-4-arduino
    ports:
      - "8341:8000"
    volumes:
      - ./app:/app/app
    restart: always